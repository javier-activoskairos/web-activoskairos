# Variables de entorno de producción.
# El ID de medición de GA4 es público (aparece en el HTML del cliente), por eso
# se versiona aquí. Si EasyPanel define NEXT_PUBLIC_GA_ID, ese valor tiene
# prioridad sobre este fichero durante `next build`.
NEXT_PUBLIC_GA_ID=G-5FJ720D0R7
